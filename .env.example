# WNBA Predictions — Environment Variables
# Copy this file to .env and fill in your values.

# The Odds API key — get one free at https://the-odds-api.com/
# Only needed if you want live today's lines via `python predict.py predict`.
# All historical training data comes from Action Network (no key required).
ODDS_API_KEY=your_odds_api_key_here
